[
  {
    "id": "anchor_id",
    "name": "anchor_id",
    "label": "Anchor ID",
    "required": true,
    "locked": false,
    "type": "text",
    "validation_regex": "^#?[a-zA-Z][a-zA-Z0-9_-]*$",
    "validation_error_message": "Start with a letter. Use only letters, numbers, hyphens and underscores.",
    "help_text": "Link to this with #your-id (e.g. #contact). Must be unique on the page.",
    "placeholder": "contact",
    "default": ""
  },
  {
    "id": "offset",
    "name": "offset",
    "label": "Extra scroll offset (px)",
    "required": false,
    "locked": false,
    "type": "number",
    "display": "text",
    "step": 1,
    "min": 0,
    "help_text": "Extra space above the target when scrolling. Sticky header height is already included via --header-height.",
    "default": 0
  }
]
